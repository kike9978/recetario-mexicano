---
import BaseLayout from "./BaseLayout.astro";

const { title } = Astro.props;
---

<BaseLayout pageTitle={title}>
    <div class="prose prose-headings:text-4xl">
        <slot />
    </div>
</BaseLayout>
